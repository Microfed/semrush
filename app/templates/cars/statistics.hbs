<div class="row">
    <div class="col-md-6">
        <h2>{{t 'cars.statistics.chart-title'}}</h2>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        {{#if model.length}}
            {{car-picking-statistics data=model}}
        {{else}}
            <h3>{{t 'cars.statistics.no-chart-data'}}</h3>
        {{/if}}
    </div>
</div>

{{outlet}}

<div class="row">
    <div class="col-md-6">
        <h2>Brand over picking history</h2>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        {{#if model.length}}
            {{car-picking-statistics data=model}}
        {{else}}
            <h3>There is not actual statistics for now. Come back later!</h3>
        {{/if}}
    </div>
</div>

{{outlet}}

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="page-header">
                <h1>{{t 'application.header'}}</h1>

                {{#unless isStorageAvailable}}
                    <h3 class="text-danger">
                        {{t 'application.localstorage-error'}}
                    </h3>
                {{/unless}}
            </div>
        </div>
    </div>

    {{outlet}}
</div>
